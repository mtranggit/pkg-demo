{
  "name": "@mtrangio/mtd-pkg",
  "version": "0.3.4",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts --out-dir dist",
    "lint": "tsc",
    "release": "pnpm build && pnpm changeset publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Michael Trang",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
